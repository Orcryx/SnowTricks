<div class="pagination">
	{% if currentPage > 1 %}
		<button class="btn" onclick="loadPage({{ currentPage - 1 }})">Réduire</button>
	{% endif %}

	{% if currentPage < totalPages %}
		<button class="btn" onclick="loadPage({{ currentPage + 1 }})">Voir plus</button>
	{% endif %}
</div>
